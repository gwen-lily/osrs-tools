[build-system]
requires = ["setuptools>57.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "osrs-tools"
version = "2.0.0"

[tool.setuptools.packages.find]
where = ["src"]
namespace = false
